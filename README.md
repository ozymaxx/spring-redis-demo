Spring Boot and Redis / Spring Boot ve Redis
--------------------------------------------

Build & tests (derleme & testler): `./gradlew build`

Build only (sadece derleme): `./gradlew build -x test`

Tests only (sadece testler): `./gradlew test`